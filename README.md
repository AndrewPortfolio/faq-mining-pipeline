# Faq-mining-pipeline
Given email text data, this program will strip PII's and clean the data. It will then be converted into vector embeddings using Ollama (nomic-embed-text), and use HDBSCAN to find FAQs. 
